const mongoose = require('mongoose')

const mongoose = new mongoose.Schema() ({
    {title = String, required = true}
    {description = String, required = true}
    {priority = String, required = true}
});

module.mongoose = model.mongoose(mongoose.schema)



